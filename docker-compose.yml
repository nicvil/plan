version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: linkiq-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: linkiq
      POSTGRES_PASSWORD: linkiq
      POSTGRES_DB: linkiq
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: linkiq-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

volumes:
  postgres_data:
  redis_data:
